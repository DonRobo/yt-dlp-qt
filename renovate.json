{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "The Python version used by the build and PySide6 have to move together: the build can only adopt a new Python once PySide6 publishes wheels for it. Grouping them into one PR lets the build job be the gate — a bump that cannot work stays red and unmerged, rather than being pinned forever and quietly going stale.",
      "matchPackageNames": ["python", "PySide6"],
      "groupName": "python runtime"
    }
  ]
}
